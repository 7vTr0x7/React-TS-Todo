import { Paper } from "@mui/material";

type PropType = {
  todo: TodoItemType;
};

const TodoItem = ({ todo }: PropType) => {
  return (
    <div>
      <Paper>app</Paper>
    </div>
  );
};

export default TodoItem;
